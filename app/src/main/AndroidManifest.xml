<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".AmazonTrackerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="Amazon Price Tracker"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmazonTracker"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.amazon.com" />
                <data android:scheme="https" android:host="www.amazon.co.uk" />
                <data android:scheme="https" android:host="www.amazon.de" />
                <data android:scheme="https" android:host="www.amazon.ca" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.PriceHistoryActivity"
            android:label="Price History"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.AlertSettingsActivity"
            android:label="Price Alerts"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.TrackedProductsActivity"
            android:label="Tracked Products"
            android:parentActivityName=".ui.MainActivity" />

        <service
            android:name=".service.PriceCheckWorker"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

    </application>

</manifest>
